package world

class World {
    private val entityList : List<Entity> = emptyList()

    fun update() {
        for (entity in entityList) {
            
        }
    }
//        for(entity in entityList){}

}